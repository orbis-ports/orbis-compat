/* Empty. u_cpu_detect.c includes it under DETECT_OS_BSD for machine-dependent CPU identification; on
   this platform the CPU is known (Liverpool, a Jaguar) and the detection falls back to CPUID, which
   needs nothing from here. */
#pragma once
