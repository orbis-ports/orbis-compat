# NOTICE — everything the orbis-sdk bundle redistributes, and under what terms

**Generated by `scripts/release/sdk-licenses.sh notice` — do not edit by hand.**
`scripts/release/sdk-licenses.sh verify` fails when this file is stale, so it cannot drift
from the table that produced it.

This file exists because of one change of role. Until the first bundle, this repository only
**consumed** the OpenOrbis PS4 Toolchain: the consumer fetched that tarball themselves, and
whether it carried the notices its own components require was upstream's business. A bundle
**redistributes**, and MIT's *"The above copyright notice … shall be included in all copies
or substantial portions of the Software"* and Apache-2.0 §4(a)/(d) then bind whoever ships
the copy. That is us.

⚠ **The SDK tarball does not discharge them.** Measured over `toolchain-llvm-18.tar.gz`
(release v0.5.4) unpacked: the entire tree contains **eight** files whose name says licence —
the root `LICENSE` (GPL-3.0), three byte-identical `bin/*/LICENSE.txt` (LGPL-3.0),
`include/stb/LICENSE`, `include/stb/data/herringbone/license.txt`,
`include/stb/tests/pngsuite/PngSuite.LICENSE`, and `include/SDL2/SDL_copying.h`. There is no
musl `COPYRIGHT`, no LLVM `LICENSE.TXT` and no `FTL.TXT` anywhere in it. Upstream's own
README says it knows: *"The accompanying LLVM binaries are licensed under the Apache 2.0
license and is owned by LLVM. Under that license, redistribution is allowed."* — true, and
the same licence also requires the copy to carry the licence. It does not.

`licenses/` in this repository, and `licenses/` in every bundle cut from it, is that gap
closed. Every file named below is the real upstream text, gathered at the pin recorded
beside it and checksummed in `licenses/SHA256SUMS`.

**This notice is not a licence.** Each component keeps the terms it arrived under. Taking a
file out of a bundle means complying with *that file's* licence, not with this repository's
`LICENSE`.

## The bundle, component by component

| Component | In the bundle | Terms | Licence text shipped | Upstream | Pinned at |
|---|---|---|---|---|---|
| `sdk-core` | `sdk/link.x`, `sdk/include/orbis/**` | **GPL-3.0-only** | `licenses/GPL-3.0.txt` | https://github.com/OpenOrbis/OpenOrbis-PS4-Toolchain | `v0.5.4` |
| `sdk-crt-musl` | `sdk/lib/crt1.o`, `sdk/lib/crti.o`, `sdk/lib/crtn.o`, `sdk/lib/crt_dyn.o`, `sdk/lib/rcrt1.o`, `sdk/lib/Scrt1.o` | **MIT** | `licenses/MIT-musl.txt` | https://github.com/OpenOrbis/musl | `2b0bb8ce2fa64ac255cb656785729d159a5f074c` |
| `sdk-crt-lib` | `sdk/lib/crtlib.o`, `sdk/src/crt/**` | **GPL-3.0-only** | `licenses/GPL-3.0.txt` | https://github.com/OpenOrbis/OpenOrbis-PS4-Toolchain | `v0.5.4` |
| `musl` | `sdk/lib/libc.a`, `sdk/include/*.h`, `sdk/include/bits/**`, `sdk/include/sys/**`, `sdk/include/arpa/**`, `sdk/include/net/**`, `sdk/include/netinet/**`, `sdk/include/netpacket/**`, `sdk/include/scsi/**` | **MIT** | `licenses/MIT-musl.txt` | https://github.com/OpenOrbis/musl | `2b0bb8ce2fa64ac255cb656785729d159a5f074c` |
| `llvm-libcxx` | `sdk/lib/libc++.a`, `sdk/lib/libc++experimental.a`, `sdk/include/c++/v1/**` | **Apache-2.0 WITH LLVM-exception** | `licenses/LLVM-libcxx-LICENSE.TXT` | https://github.com/llvm/llvm-project | `llvmorg-11.0.0` |
| `llvm-libcxxabi` | `sdk/lib/libc++abi.a` | **Apache-2.0 WITH LLVM-exception** | `licenses/LLVM-libcxxabi-LICENSE.TXT` | https://github.com/llvm/llvm-project | `llvmorg-11.0.0` |
| `llvm-libunwind` | `sdk/lib/libunwind.a` | **Apache-2.0 WITH LLVM-exception** | `licenses/LLVM-libunwind-LICENSE.TXT` | https://github.com/llvm/llvm-project | `llvmorg-11.0.0` |
| `llvm-builtins` | `sdk/lib/libclang_rt.builtins-x86_64.a` | **Apache-2.0 WITH LLVM-exception** | `licenses/LLVM-compiler-rt-LICENSE.TXT` | https://github.com/llvm/llvm-project | `llvmorg-11.0.0` |
| `sdk-stubs` | `sdk/lib/*.so` | **UNRESOLVED - see LICENSING.md** | — (none exists; see below) | https://github.com/OpenOrbis/ps4libdoc | `archived, no LICENSE` |
| `sdk-oo-tools` | `sdk/bin/*/create-fself*`, `sdk/bin/*/create-gp4*`, `sdk/bin/*/readoelf*` | **GPL-3.0-only** | `licenses/GPL-3.0.txt` | https://github.com/OpenOrbis/create-fself | `v0.5.4` |
| `sdk-pkgtool` | `sdk/bin/*/PkgTool.Core*`, `sdk/bin/*/LibOrbisPkg*.dll`, `sdk/bin/*/PkgEditor.exe` | **LGPL-3.0-only** | `licenses/LGPL-3.0.txt` | https://github.com/maxton/LibOrbisPkg | `bundled with v0.5.4` |
| `sdl2` | `sdk/include/SDL2/**`, `sdk/lib/libSDL2.a`, `sdk/lib/libSDL2main.a` | **Zlib** | `licenses/Zlib-SDL.txt` | https://github.com/libsdl-org/SDL | `release-2.0.9` |
| `sdl2-image` | `sdk/include/SDL2/SDL_image.h`, `sdk/lib/libSDL2_image.a` | **Zlib** | `licenses/Zlib-SDL_image.txt` | https://github.com/libsdl-org/SDL_image | `release-2.0.5` |
| `sdl2-ttf` | `sdk/include/SDL2/SDL_ttf.h` | **Zlib** | `licenses/Zlib-SDL_ttf.txt` | https://github.com/libsdl-org/SDL_ttf | `release-2.0.15` |
| `sdl2-khronos` | `sdk/include/SDL2/SDL_opengl_glext.h`, `sdk/include/SDL2/SDL_opengles2_*.h`, `sdk/include/SDL2/SDL_vulkan.h` | **MIT (Khronos) and Zlib (SDL_vulkan.h)** | — (none exists; see below) | https://github.com/KhronosGroup/OpenGL-Registry | `shipped in include/SDL2` |
| `freetype` | `sdk/include/freetype/**`, `sdk/include/ft2build.h` | **FTL (dual FTL OR GPL-2.0, choice undocumented)** | `licenses/FTL.TXT` | https://github.com/freetype/freetype | `VER-2-5-1` |
| `stb` | `sdk/include/stb/**` | **MIT OR Unlicense** | `licenses/MIT-stb.txt` | https://github.com/nothings/stb | `bundled with v0.5.4` |
| `orbis-compat` | `orbis-compat/include/**`, `orbis-compat/src/**`, `orbis-compat/optional/**`, `orbis-compat/vkloader/**`, `orbis-compat/cmake/*.cmake`, `orbis-compat/scripts/**`, `orbis-compat/test/**`, `orbis-compat/build/liborbis-compat.a` | **MIT** | `licenses/MIT-orbis-compat.txt` | https://github.com/orbis-ports/orbis-compat | `see BUNDLE.txt` |
| `compat-crt` | `orbis-compat/crt/**`, `orbis-compat/build/crt/*.o` | **MIT** | `licenses/MIT-orbis-compat.txt` | https://github.com/orbis-ports/orbis-compat | `see BUNDLE.txt` |
| `compat-tls-ld` | `toolchain/orbis-tls.ld`, `orbis-compat/cmake/orbis-tls.ld` | **GPL-3.0-only** | `licenses/GPL-3.0.txt` | https://github.com/OpenOrbis/OpenOrbis-PS4-Toolchain | `v0.5.4 link.x` |
| `compat-ioccom` | `orbis-compat/include/sys/ioccom.h` | **BSD-3-Clause** | `licenses/BSD-3-Clause-FreeBSD.txt` | https://github.com/freebsd/freebsd-src | `release/9.0.0` |
| `mesa` | `mesa/build-orbis/**`, `mesa/include/**` | **MIT** | `licenses/MIT-mesa.txt` | https://gitlab.freedesktop.org/mesa/mesa | `see BUNDLE.txt` |
| `mesa-khronos` | `mesa/include/vulkan/**`, `mesa/include/EGL/**`, `mesa/include/KHR/**`, `mesa/include/GLES*/**` | **Apache-2.0** | `licenses/Apache-2.0-Khronos.txt` | https://github.com/KhronosGroup | `shipped in the Mesa tree` |
| `mesa-zlib` | `mesa/build-orbis/subprojects/zlib-*/libz.a` | **Zlib** | `licenses/Zlib-zlib.txt` | https://github.com/madler/zlib | `v1.3.1` |

## Why each row is what it is

* **`sdk-core`** — The SDK's own headers and linker script. No per-file notices anywhere; the root LICENSE is all there is.
* **`sdk-crt-musl`** — Six of the seven startup objects, built from musl's crt/ps4 and crt/. Identified by each object's STT_FILE and exported symbols, not by its filename.
* **`sdk-crt-lib`** — The PRX module stub, and the ONLY GPL-3.0 crt object. Its source ships beside it at sdk/src/crt/crtlib.c. It is not on an executable's link line.
* **`musl`** — The libc and its headers. OpenOrbis' fork rather than upstream musl - that is the tree these bytes came from.
* **`llvm-libcxx`** — libc++ and its 180 headers. _LIBCPP_VERSION 11000 is the measurement that picks the pin.
* **`llvm-libcxxabi`** — 21 members. Its LICENSE.TXT is not byte-identical to libc++'s.
* **`llvm-libunwind`** — Also present INSIDE libc++.a as libunwind.cpp.o and Unwind-sjlj.c.o - the same code, twice.
* **`llvm-builtins`** — 159 members. compiler-rt's own text, which differs from the other three.
* **`sdk-stubs`** — ~422 generated import libraries with empty bodies. Their generator and their input data carry no licence at all.
* **`sdk-oo-tools`** — Go binaries; `strings` finds github.com/OpenOrbis/create-fself/pkg/fself inside create-fself. ⚠ Only bin/LICENSE.txt sits beside them and that is the LGPL, which is LibOrbisPkg's, not theirs.
* **`sdk-pkgtool`** — bin/README.md: "All code in this repository is licensed under the GNU LGPL version 3". This one IS correctly noticed upstream.
* **`sdl2`** — 86 headers plus two archives. The headers carry the Zlib text inline; the ARCHIVES carry nothing, which is why a separate file ships.
* **`sdl2-image`** — A separate project that sits in the same include directory. Its header declares 2.0.6 and upstream has no reachable tag of that name - see LICENSING.md.
* **`sdl2-ttf`** — A third project in the same directory, © 2001-2020. ⚠ Header only - there is no libSDL2_ttf.a in the SDK's lib/, so nothing links against it.
* **`sdl2-khronos`** — inline - each file carries its own complete licence text, so no separate file is required.
* **`freetype`** — 88 headers, FREETYPE_MAJOR 2 / MINOR 5, "Copyright 1996-2013". FTL.TXT is NOT in the SDK tarball.
* **`stb`** — The only third-party component in the tarball whose notice was already present.
* **`orbis-compat`** — The overlay, minus the two files below which are NOT MIT. Since 2026-09-18 the cmake/*.cmake and vkloader/** staged under this path come from https://github.com/orbis-ports/orbis-porting-kit - same MIT terms, same author, different repository; the bundle keeps the old layout because toolchain/orbis-sdk.cmake names it.
* **`compat-crt`** — orbis-compat's own startup objects, written here rather than taken from either upstream. Absent from a bundle cut before they existed.
* **`compat-tls-ld`** — Derived from the SDK's link.x. Two substantive lines differ; the rest of the diff is its own comment.
* **`compat-ioccom`** — Its macros follow FreeBSD's sys/sys/ioccom.h because they encode an ABI and cannot be written differently and still work.
* **`mesa`** — RADV plus EGL/GLES and the gallium target, and the headers they were built against. Per-file SPDX headers throughout.
* **`mesa-khronos`** — The registry headers Mesa vendors. ⚠ These are the ones a mismatched copy silently breaks - see the bundle README.
* **`mesa-zlib`** — Mesa's meson subproject, pinned by subprojects/zlib.wrap to 1.3.1. Mesa's shader disk cache needs it.

## The three things this notice does NOT establish

1. ⚠ **`sdk/lib/*.so` has no licence, and no amount of care produces one.** The ~422 import
   libraries are empty by construction — `libkernel.so` carries `.dynsym` 0x5c10 for roughly
   945 symbols against `.text` 0x2654, so no symbol has a body. They were generated by
   `OpenOrbis/orbis-lib-gen` from `OpenOrbis/ps4libdoc`, and **both of those repositories are
   archived with no `LICENSE` file at all**. There is no grant to quote and therefore no
   notice to ship. A bundle containing them is shipping files whose redistribution terms
   nobody has stated. That is recorded, not resolved.
2. ⚠ **`create-fself`, `create-gp4` and `readoelf` ship without corresponding source.** They
   are GPL-3.0-only Go binaries from OpenOrbis' own repositories — `strings` finds
   `github.com/OpenOrbis/create-fself/pkg/fself` inside the first — and the tarball carries
   no source for any of them. GPL-3.0 §6 wants the source with the object or a written offer
   in its place. `NOTICE.md` records the upstream repository; that is a pointer, not an offer.
   (The crt objects are **not** in this category: six of the seven are musl's and MIT, and the
   one that is GPL-3.0, `crtlib.o`, has its source shipped beside it at `sdk/src/crt/crtlib.c`.
   `LICENSING.md` §2.9 has the measurement that separates them.)
3. ⚠ **FreeType's dual choice is undocumented and this notice does not invent one.**
   FreeType is FTL **or** GPL-2.0 at the recipient's option. 86 of the 88 shipped headers
   point at `LICENSE.TXT` — the FTL — and no file in the tarball mentions GPL-2.0. `FTL.TXT`
   is shipped because it is the licence the shipped files actually name. Nothing here says
   OpenOrbis made a choice, because nothing in their tree says they did.

## Regenerating

```sh
scripts/release/sdk-licenses.sh fetch     # gather the texts at their pins (network)
scripts/release/sdk-licenses.sh sums      # rewrite licenses/SHA256SUMS
scripts/release/sdk-licenses.sh notice    # rewrite this file
scripts/release/sdk-licenses.sh verify    # offline: the gate CI runs
```

Generated 2026-09-18.
